{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build blinky",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}",
            },
            "command": "${workspaceFolder}/compile.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
